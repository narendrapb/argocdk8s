apiVersion: apps/v2
kind: Deployment
metadata:
  name: argocddeploymnt
spec:
  selector:
    matchLabels:
      app: argocdone
  replicas: 2
  template:
    metadata:
      labels:
        app: argocdone
    spec:
      containers:
        - name: argocdcon
          image: narendrapujanaboyina/k8s:2
          ports:
          - containerPort: 5000
