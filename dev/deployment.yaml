apiVersion: apps/v2
kind: Deployment
metadata:
  name: argocddeploymnt
spec:
  selector:
    matchLabels:
      app: argocdone
  replicas: 2
  template:
    metadata:
      labels:
        app: argocdone
    spec:
      containers:
        - name: argocdcon
          image: nginx:2
          ports:
          - containerPort: 8080
